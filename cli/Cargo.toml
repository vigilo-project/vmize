[package]
name = "vmize"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "vmize"
path = "src/main.rs"

[dependencies]
batch = { path = "../batch" }
dashboard = { path = "../dashboard" }
clap = { version = "4.5", features = ["derive"] }
tokio = { version = "1", features = ["full"] }
