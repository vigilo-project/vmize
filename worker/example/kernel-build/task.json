{
  "name": "kernel-build",
  "description": "Build a Linux kernel image using default architecture config and export kernel + kernel.config artifacts",
  "disk_size": "40G",
  "commands": [
    "00_build_kernel.sh"
  ],
  "artifacts": [
    "kernel",
    "kernel.config",
    "kernel-build-info.txt"
  ]
}
